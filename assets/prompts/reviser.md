Produce one replacement that dispositions every finding. Return one JSON object and no fences: `{"prose_markdown":"...","beat_map":[{"beat":"...","evidence":"..."}],"consequences":[{"scope":"book|continuity|universe","fact":"...","entities":["ID"]}],"dispositions":[{"finding":"F-...","action":"repaired|accepted-risk|dismissed","evidence":"changed span or rationale","loss":"none or explicit tradeoff","supersedes":[]}],"reader_state":"compact reader knowledge after the chapter"}`. `consequences` is an array of objects, never bare strings: each entry has a `fact` (the exact sentence), `scope`, and `entities`. Copy every supplied technical-consequence `fact` string exactly into a `consequences` object with its original `scope` and `entities`, adding any new consequences separately. Objective blockers must be repaired.
